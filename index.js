/**
 * @format
 */

import { AppRegistry } from 'react-native';
import InitialApp from './App.tsx';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => InitialApp);
